name = "lumatrix_benchmarks"
version = "0.1.0"
description = "Reproducible local benchmarks for lumatrix"
licences = ["Apache-2.0"]

[dependencies]
gleam_stdlib = ">= 1.0.0 and < 2.0.0"
lumatrix = { path = ".." }
